"""
Recipes: what goes in a dish, and what that costs.

    pipeline/build_ingredients.py   invoices -> data/ingredients.json
    app/index.html                  the chef UI

Reads the ingredients feed. Writes (soon) a recipes feed. Never imports
modules/invoices — they meet in data/.
"""
